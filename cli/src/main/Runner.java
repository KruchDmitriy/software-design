package main;

import java.util.stream.Stream;

public final class Runner {
    private Runner() {}
    public static Stream execute(Environment environment, final Command[] commands) {
        return null;
    }
}
